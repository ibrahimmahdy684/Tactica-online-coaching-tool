const LoadingSpinner = () => {
  return (
    <div style={{ textAlign: "center", margin: "2rem" }}>
      <img src="/loading.svg" alt="Loading..." width={50} />
      <p>Loading...</p>
    </div>
  );
};

export default LoadingSpinner;
